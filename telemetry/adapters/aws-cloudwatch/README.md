# AWS CloudWatch

Auth: AWS CLI / profile. Never print secrets.

Logs: Logs Insights. Metrics: CloudWatch metrics. Errors: filter log groups or X-Ray if wired. No session replay. No feature flags.

Say replay is unavailable. Bring the log group, the query, and the request id.
