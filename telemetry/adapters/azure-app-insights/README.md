# Azure App Insights

Auth: Azure CLI or App Insights app id + key.

Errors and logs: Kusto/`exceptions` and `traces`. Metrics: `customMetrics` / standard request metrics. No session replay. No feature flags.

Say replay is unavailable. Bring an operation_id and the traces around it.
